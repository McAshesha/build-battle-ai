#file: noinspection PluginYml
name: ${project.name}
version: '${project.version}'
main: ${plugin.main}
api-version: '1.13'
authors: ${plugin.authors}
description: ${project.description}
website: ${project.url}
prefix: ${plugin.prefix}
load: POSTWORLD
